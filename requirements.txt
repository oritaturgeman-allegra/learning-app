# Core dependencies
python-dotenv>=1.0.0

# LLM providers
openai>=1.0.0
google-generativeai>=0.8.0        # Gemini API

# Web interface
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
httpx>=0.26.0,<1.0.0             # Required for FastAPI TestClient
jinja2>=3.1.0,<4.0.0             # Template engine for FastAPI
python-multipart>=0.0.9,<1.0.0  # Form data parsing for FastAPI

# Utilities
pydantic>=2.0.0

# RSS Feed Parsing
feedparser>=6.0.10

# Translation
deep-translator>=1.11.4

# === ERROR HANDLING & RESILIENCE ===
tenacity>=8.2.3,<9.0.0          # Retry logic with exponential backoff

# === PERFORMANCE & ASYNC ===
aiohttp>=3.9.0,<4.0.0           # Async HTTP for parallel RSS fetching
cachetools>=5.3.0,<6.0.0        # In-memory caching with TTL

# === DATABASE ===
sqlalchemy>=2.0.0,<3.0.0        # ORM
alembic>=1.13.0,<2.0.0          # Database migrations
aiosqlite>=0.19.0,<1.0.0        # Async SQLite support
psycopg2-binary>=2.9.9,<3.0.0   # PostgreSQL adapter (for Supabase)

# === AUTHENTICATION ===
bcrypt>=4.0.0,<5.1.0            # Password hashing
email-validator>=2.0.0,<3.0.0   # Pydantic EmailStr validation

# === LOGGING & MONITORING ===
structlog>=24.1.0,<25.0.0       # Structured logging
python-json-logger>=2.0.7,<3.0.0  # JSON formatted logs
sentry-sdk[fastapi]>=2.0.0,<3.0.0  # Error monitoring

# === SCHEDULING ===
apscheduler>=3.10.0,<4.0.0      # Background job scheduling
